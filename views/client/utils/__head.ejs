<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <% try{ %><% if (metaTags) { %>
    <meta name="description" content="<%=  productInformation.shortDescription %>">
    <meta name="keywords" content="capnin,<% productInformation.searchKeyWord.forEach(element => { %><%= ","+element %> <% }) %>">
    <meta name="author" content="Kanishk Ranjan"></meta><% } %><% } catch{} %>
    <link rel="stylesheet" href="<%= cssPath %> ">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.8.2/css/all.min.css" crossorigin="anonymous" />
    <link href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&amp;display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Quicksand&family=Roboto+Slab:wght@500&display=swap" rel="stylesheet"> 
</head>
